import React from 'react';
import WebDev from './assets/web_dev.png';

const Header = (props) => {
    return (
        <header>
            <img src={WebDev}/>
        </header>
    )
}

export default Header;